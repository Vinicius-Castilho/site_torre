// const emblaNode = document.querySelector('#historia-imagem');
// const prevBtn = emblaNode.querySelector('.embla__prev');
// const nextBtn = emblaNode.querySelector('.embla__next');

// const options = { loop: true, duration: 25 };
// const plugins = [EmblaCarouselFade()]; 

// const emblaApi = EmblaCarousel(emblaNode, options, plugins);

// // Listeners para os botões manuais
// prevBtn.addEventListener('click', () => emblaApi.scrollPrev(), false);
// nextBtn.addEventListener('click', () => emblaApi.scrollNext(), false);

// // Autoplay (Opcional: você pode remover se quiser apenas manual)
// setInterval(() => {
//   if (emblaApi) emblaApi.scrollNext();
// }, 5000);